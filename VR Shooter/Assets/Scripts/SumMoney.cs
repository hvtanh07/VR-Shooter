using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SumMoney : MonoBehaviour
{
    public int Money = 0;
   // public Player player;
   // public TextMesh sumMoney;
    // Start is called before the first frame update
    void Start()
    {
        //Money += player.sumMoney;
    }

    // Update is called once per frame
    void Update()
    {
       // sumMoney.text = Money.ToString();
    }
}
